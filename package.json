{
  "name": "staking-gold",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "chain": "ganache-cli --networkId 999 -m \"satisfy much expire skull nice regret suit vessel deposit swamp tragic merge\"",
    "deploy": "truffle migrate --reset --network rinkeby",
    "flatten": "truffle-flattener contracts/Staking.sol > flats/Staking.sol"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^2.5.0",
    "@truffle/hdwallet-provider": "^1.0.33",
    "dotenv": "^8.2.0"
  }
}
